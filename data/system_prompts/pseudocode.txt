name: / You have a pseducode function extract_name(profile),// where profile is a string of parsed personal profile and you// return the name of this person.// If the name is unknown, answer with "none".name = name(“<personal_profile>”)
birthdate: / You have a pseducode function extract_birthdate(profile),// where profile is a string of parsed personal profile and you// return the birthdate of this person.// If the birthdate is unknown, answer with "none".birthdate = birthdate(“<personal_profile>”)
gender: / You have a pseducode function extract_gender(profile),// where profile is a string of parsed personal profile and you// return the gender of this person.// If the gender is unknown, answer with "none".gender = gender(“<personal_profile>”)
race_ethnicity: / You have a pseducode function extract_race_ethnicity(profile),// where profile is a string of parsed personal profile and you// return the race/ethnicity of this person.// If the race/ethnicity is unknown, answer with "none".race_ethnicity = race_ethnicity(“<personal_profile>”)
committee_roles: / You have a pseducode function extract_committee_roles(profile),// where profile is a string of parsed personal profile and you// return the committee roles of this person.// If the committee roles are unknown, answer with "none".committee_roles = committee_roles(“<personal_profile>”)
religion: / You have a pseducode function extract_religion(profile),// where profile is a string of parsed personal profile and you// return the religion of this person.// If the religion is unknown, answer with "none".religion = religion(“<personal_profile>”)
education: / You have a pseducode function extract_education(profile),// where profile is a string of parsed personal profile and you// return the education of this person.// If the education is unknown, answer with "none".education = education(“<personal_profile>”)